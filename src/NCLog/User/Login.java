package NCLog.User;

import javax.swing.JOptionPane;

public class Login {

    /*Usuario us = new Usuario();*/
    
    
    /* int opt;
        do{
            opt = Integer.parseInt(JOptionPane.showInputDialog(String.format("Digite uma Opção:%n1 - Fazer Login%n2 - Cadastrar usuário%n0 - sair")));

        switch (opt) {
            case 1:
                us.loginUsuario();
                us.password();

                if (us.login()) {
                    JOptionPane.showMessageDialog(null, "Logado com sucesso", "Login realizado", JOptionPane.INFORMATION_MESSAGE);
                } else {
                    JOptionPane.showMessageDialog(null, "Usuário ou senha inválidos", "ERRO", JOptionPane.ERROR_MESSAGE);
                }
                break;
            case 2:
                us.loginUsuario();
                us.password();
                us.dicaSenha();

                if (us.cadastroUsuario()) {
                    JOptionPane.showMessageDialog(null, "Usuário cadastrado com sucesso" + us.getNome_usuario(), "Sucesso", JOptionPane.INFORMATION_MESSAGE);
                } else {
                    JOptionPane.showMessageDialog(null, "Erro ao cadastrar usuário", "ERRO", JOptionPane.ERROR_MESSAGE);
                }
                break;
            default:
                opt = 0;

        }        
    }while (opt != 0);*/

}
